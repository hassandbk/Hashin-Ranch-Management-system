package com.example.hashinfarm.controller.handlers.cattleDetailsMoreHandlers;

import com.example.hashinfarm.controller.handlers.ButtonActionHandler;

    public class ModifyCowHandler extends ButtonActionHandler {

        public ModifyCowHandler() {
            super("modifyCow", (event, gridPane) -> {
                // Additional logic specific to the "addBreed" button
            });
        }
    }

