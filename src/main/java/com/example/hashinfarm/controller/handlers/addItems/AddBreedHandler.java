package com.example.hashinfarm.controller.handlers.addItems;

import com.example.hashinfarm.controller.handlers.ButtonActionHandler;
import javafx.event.ActionEvent;
import javafx.scene.layout.GridPane;

public class AddBreedHandler extends ButtonActionHandler {

    public AddBreedHandler() {
        super("addBreed", (event, gridPane) -> {
            // Additional logic specific to the "addBreed" button
        });
    }
}