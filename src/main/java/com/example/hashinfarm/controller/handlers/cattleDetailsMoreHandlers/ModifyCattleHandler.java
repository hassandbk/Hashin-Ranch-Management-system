package com.example.hashinfarm.controller.handlers.cattleDetailsMoreHandlers;

import com.example.hashinfarm.controller.handlers.ButtonActionHandler;

    public class ModifyCattleHandler extends ButtonActionHandler {

        public ModifyCattleHandler() {
            super("modifyCattle", (event, gridPane) -> {
                // Additional logic specific to the "modifyCattle" button
            });
        }
    }

